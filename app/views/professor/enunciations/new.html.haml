.header.block
  %h1
    Novo Enunciado
.block
  = simple_form_for [:professor, @crowd.subject, @crowd, @enunciation], html: { class: 'form-horizontal' } do |f|
    = render :partial => 'form', locals: { f:f }
    .form-actions
      = f.submit class: 'btn btn-primary'
