.header.block
  %h1 Editar Enunciado
.block
  = simple_form_for [:professor, @enunciation.crowd.subject, @enunciation.crowd, @enunciation], html: { class: 'form-horizontal' } do |f|
    = render :partial => 'form', locals: { f:f }
    .form-actions
      = f.submit class: 'btn btn-primary'
