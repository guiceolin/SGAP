.header.block
  %h1
    Editar Estudante
.block
  = simple_form_for [:admin, @student], html: { class: 'form-horizontal' } do |f|
    = render partial: 'form', locals: { f:f }
    .form-actions
      = f.submit class: 'btn btn-primaty'
