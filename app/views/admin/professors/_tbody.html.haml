- @professors.each do |p|
  %tr
    %td
      = p.name
    %td
      = p.username
    %td
      = p.email
    %td
      =link_to content_tag(:i, '', class: 'icon-pencil'), edit_admin_professor_path(p)
      =link_to content_tag(:i, '', class: 'icon-remove'), admin_professor_path(p), method: :delete, confirm: 'Tem Certeza?'
