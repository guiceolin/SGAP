.header.block
  %h1
    Editar Disciplina
.block
  = simple_form_for [:admin, @subject], html: { class: 'form-horizontal' } do |f|
    =render partial: 'form', locals: { f: f }
    .form-actions
      = f.submit class: 'btn btn-primary'
